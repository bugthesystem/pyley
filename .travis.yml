language: python

python:
  - "2.7"
  - "3.3"
  - "3.4"

# command to install dependencies
before_script:
  - /bin/bash ./.travis.sh
install:
  - "pip install ."
  - "pip install pytest"
  - "pip install requests"

script: py.test
